framework:
    secret: '%env(APP_SECRET)%'
    handle_all_throwables: true
    php_errors:
        log: true
    messenger:
        transports:
            async: 'in-memory://'

when@test:
    framework:
        test: true
